# Deploy + verify (contracts) — dari contracts/ (contracts/.env adalah symlink ke file ini):
#   forge script script/Deploy.s.sol --rpc-url giwa_sepolia --broadcast \
#     --verify --verifier blockscout --verifier-url https://sepolia-explorer.giwa.io/api
# Catatan: GIWA Sepolia (91342) belum ada di Etherscan API v2 — explorer-nya Blockscout,
# verifikasi via Blockscout API di atas, tanpa API key.
PRIVATE_KEY=
TREASURY_ADDRESS=
COLLECTION_URI=https://api.thebingofi.example/metadata/{id}.json
