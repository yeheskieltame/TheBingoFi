# Deploy (contracts) — pakai: cd contracts && forge script script/Deploy.s.sol --rpc-url giwa_sepolia --broadcast
PRIVATE_KEY=
TREASURY_ADDRESS=
COLLECTION_URI=https://api.thebingofi.example/metadata/{id}.json
